const { User } = require('../models');
// handling register
const handleRegister = (req,res) => {

}

// handling Login
const handleLogin = (req,res) => {
    
}

module.exports = {
    handleRegister,
    handleLogin,
}